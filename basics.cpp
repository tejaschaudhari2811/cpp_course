#include<iostream>
#include<string>
using namespace std;

int main()
{
    // Declaration of various variable types.
    int one = 1;
    double f = 1.2345;
    char myLetter = 'T';
    std::string myName = "Tejas";
    bool myBool = true;

    // Declare variables of the same type in one line
    int length = 50, height = 20, width = 30;

    // Assign one Value to multiple variables
    // Let us assume that we try to write a rectangle
    int side_1, side_2;
    side_1 = side_2 = 20;

    // Taking user input
    // float x;
    // std::cout<<"Enter a number ";
    // std::cin>>x;

    // std::cout<<x<<std::endl;
    // return 0;

    // Operators
    int x = 1, y = 2;
    cout<<x%y<<endl;


}